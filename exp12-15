Exp 12:
mvn -version
mvn archetype:generate
cd sample
change the code from App to Calculator or anything
mvn package
mvn clean test
Exp 14:
Step 1: Create a Simple Java Project
Step 2: Add the code in app and apptest
Step 3: Add JaCoCo Plugin in pom.xml
Add the JaCoCo plugin inside the <build> section.
<build>
    <plugins>
        <plugin>
            <groupId>org.jacoco</groupId>
            <artifactId>jacoco-maven-plugin</artifactId>
            <version>0.8.11</version>
            <executions>
                <execution>
                    <goals>
                        <goal>prepare-agent</goal>
                    </goals>
                </execution>
                <execution>
                    <id>report</id>
                    <phase>test</phase>
                    <goals>
                        <goal>report</goal>
                    </goals>
                </execution>
            </executions>
        </plugin>
    </plugins>
</build>
Step 4: Run Tests and Generate Coverage Report
Open terminal in the project directory and run:
mvn clean test
Step 5: Improve Coverage
Add a test for the exception case:
@Test
public void testDivideByZero() {
    assertThrows(IllegalArgumentException.class, () -> {
        calc.divide(4, 0);
    });
}
Run again:
mvn clean test
Commands:
Mvn clean test
Mvn clean verify
Exp:15
docker --version   
docker ps  
docker ps  -a
docker images
docker run hello-world
docker pull nginx
docker run -d -p 8083:80 nginx
docker stop <the id given>
docker rm <the id>
